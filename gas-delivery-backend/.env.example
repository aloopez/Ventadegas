# === SERVIDOR ===
PORT=3000

# === BASE DE DATOS (Aiven MySQL) ===
DB_HOST=tu-host.aivencloud.com
DB_USER=avnadmin
DB_PASSWORD=tu_password_aqui
DB_NAME=ventadegas
DB_PORT=25060

# === SEGURIDAD ===
# Genera un JWT_SECRET fuerte:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=tu_jwt_secret_super_seguro_aqui

# Llave maestra para crear nuevos usuarios admin (solo se usa en registro inicial)
SUPER_ADMIN_SECRET=tu_super_admin_secret_aqui

# === CORS (opcional, separado por comas) ===
CORS_ORIGIN=https://tu-dominio.com,http://localhost:5173
